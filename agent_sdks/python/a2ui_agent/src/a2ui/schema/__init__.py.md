# agent_sdks/python/a2ui_agent/src/a2ui/schema/__init__.py

## 개요

`a2ui.schema` 패키지의 초기화 파일이다. 파일 내용은 Apache License 헤더만 포함하며 실제 코드가 없다. 패키지를 Python 모듈로 인식시키는 역할만 수행하며, 하위 모듈(`catalog`, `catalog_provider`, `constants`, `manager` 등)을 외부로 재내보내지 않는다.

## 의존성

없음.

## Exports

없음 (빈 `__init__.py`).

## 동작 흐름

임포트 시 아무 동작도 수행하지 않는다. 패키지 내 개별 모듈을 직접 임포트하여 사용해야 한다.
