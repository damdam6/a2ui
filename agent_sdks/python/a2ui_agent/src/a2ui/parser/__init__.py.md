# agent_sdks/python/a2ui_agent/src/a2ui/parser/__init__.py

## 개요

`parser` 패키지의 초기화 파일이다. Apache 2.0 라이선스 주석만 포함하며 어떠한 심볼도 내보내지 않는다. 패키지를 Python 모듈로 인식시키는 역할만 한다.

## 의존성

- 외부 패키지: 없음
- 내부 모듈: 없음

## Exports

없음 (빈 `__init__.py`)

## 동작 흐름

파일 임포트 시 아무런 코드도 실행되지 않는다. 패키지 디렉토리 마커로만 존재한다.
