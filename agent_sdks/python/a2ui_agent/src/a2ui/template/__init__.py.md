# agent_sdks/python/a2ui_agent/src/a2ui/template/__init__.py

## 개요

`a2ui.template` 패키지의 초기화 파일로, 라이선스 헤더만 포함하고 실질적인 코드는 없다. 패키지 네임스페이스를 선언하는 역할만 하며 아무것도 import하거나 export하지 않는다.

## 의존성

없음.

## Exports

없음.

## 동작 흐름

Python이 `a2ui.template` 패키지를 임포트할 때 이 파일이 실행되지만, 사이드 이펙트나 공개 심볼 등록은 일절 없다.
