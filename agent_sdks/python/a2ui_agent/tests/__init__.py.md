# agent_sdks/python/a2ui_agent/tests/__init__.py

## 개요

`tests` 패키지의 초기화 파일로, 라이선스 헤더만 포함하고 실질적인 코드가 없다. Python이 `tests` 디렉토리를 패키지로 인식하게 하는 역할만 수행한다.

## 의존성

없음.

## Exports

없음.

## 동작 흐름

임포트 시 아무 동작도 하지 않는다.
